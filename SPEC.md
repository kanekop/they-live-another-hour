# They Live Another Hour — 仕様書 v0.1

作成: 2026-08-16（Kaneko の秘書エージェントが起案・以後の開発と本書の育成は本プロジェクト担当エージェントに委ねる）

## 1. コンセプト

現実世界の「時刻を表示しているもの」にカメラをかざすと、その表示が **Another Hour の時刻に置き換わって見える**レンズアプリ。Google Lens のように、カメラ越しの世界をリアルタイムに書き換える。

- 対象: デジタル時計・電子レンジやレコーダーの時刻表示・駅の発車標・PC/スマホ画面上の時計など、**数字で時刻を表示しているもの**（Phase 1）。アナログ文字盤は Phase 2。
- 体験の核: 「自分の一日が Another Hour で流れていたら、いま世界はこう見える」を、**自分の道具（スマホ）越しに現実へ重ねる**こと。

## 2. Another Hour ルールの正本（必読）

ルールはこの仕様書に**複製しない**。正本は以下。

- 仕様: `../another-hour-clock/spec/another-hour-clock-spec-v2.5.md`
- リファレンス実装: `../another-hour-clock/index.html`（換算は純関数として書かれており、そのまま流用してよい・MIT）

最低限の要約（詳細・端数処理・エッジケースは必ず正本に従う）:

- **Compressed Period**: real 00:00–23:00 → 表示は 24/23 倍（≈1.04348）で進む。例: real 12:00 → 表示 約12:31。
- **Another Hour**: real 23:00–24:00 → 表示は 1:00 に跳び、**自然速度で 0:00 へ逆行**。例: real 23:30 → 表示 0:30。real 24:00 で表示 0:00 に着地し翌日へ継ぐ。

## 3. 換算の定義（本アプリ固有の仕様判断）

- カメラに映る時計の**表示時刻を real time とみなして**写像する（その時計が進んでいよう遅れていようが、写っている値を入力とする）。
- 秒が見えない表示は分単位で換算する。秒まで見える表示（HH:MM:SS）は秒まで換算してよい。
- 23時台（Another Hour 区間）は、換算表示に**区間が違うことが分かる視覚差**（色・小さなラベル等）を付ける。ここは Another Hour の見せ場。
- 12時間表記（AM/PM）の入力は Phase 1 では「映った数字をそのまま 0–11時として扱う」でよい（正しい真昼/真夜中の判定は端末時刻を補助に使ってよい）。判断に迷ったら Kaneko に確認。

検収に使うテスト値（spec v2.5 準拠）:

| 映った表示 | 換算表示 |
|---|---|
| 12:00 | 12:31 |
| 22:59 | 23:59 |
| 23:00 | 1:00（以後逆行） |
| 23:30 | 0:30 |
| 06:00 | 6:15 |

## 4. ロードマップ

- **Phase 1（まずこれだけ）**: Web アプリで**デジタル表示**の置き換えを実現する。
- **Phase 2**: アナログ文字盤対応（針の検出→時刻推定→針の描き換え or 併記）。
- **Phase 3**: iOS ネイティブアプリ化。

## 5. Phase 1 スコープ

### やること

1. スマホブラウザ（iPhone Safari 実機を一次ターゲット）でカメラ映像を表示。
2. 映像内の時刻らしい数字列（`HH:MM` / `HH:MM:SS`・7セグメント表示を含む）をリアルタイム検出。
3. 検出した時刻を §3 のルールで換算し、**元の表示に重ねて**（AR 風・対象に追従して）表示。
4. すべて端末内で処理する。**映像・画像を外部へ送信しない**（プライバシー）。

### やらないこと

- アナログ文字盤（Phase 2）。
- iOS ネイティブ（Phase 3）。
- 時刻以外の数字（価格・電話番号等）への発火は誤検出として抑制する側。

### 性能目安

- 検出更新 5fps 以上を目標、オーバーレイ描画は毎フレーム追従。
- かざしてから初回の置き換え表示まで 1 秒以内。

### 技術選定

実装者に委ねる（候補メモ: `getUserMedia`＋canvas オーバーレイ、OCR は tesseract.js／TensorFlow.js の7セグ・数字検出／MediaPipe 等。単一ページ・依存最小の構成は another-hour-clock の流儀と相性がよい）。

## 6. Phase 1 受け入れ基準

1. iPhone の Safari で URL を開き、カメラ許可だけで動く（インストール・ビルド不要）。
2. 目の前のデジタル時計（または PC 画面に表示した時計）にかざすと、1秒以内に換算表示が重なり、追従する。
3. 換算値が §3 のテスト表とすべて一致する。
4. 23時台にかざすと Another Hour 区間の視覚差が出る。
5. 時刻でない数字への誤発火が体感で邪魔にならない。

## 7. 未決事項（実装前に Kaneko と確認するとよい点）

- オーバーレイの見せ方: 元表示を**覆って置換**するか、**並記**（元の隣に AH 時刻）か。
- Another Hour 区間の演出（色・ラベル文言）。
- アプリ名の表記（They Live Another Hour / 略称）。名称は Another Hour™ の商標ポリシー `../another-hour-clock/TRADEMARK.md` に従う（本人のプロジェクトなので問題なし）。

## 8. 体制

- 開発: 本プロジェクト担当エージェント（このフォルダが作業領域・本書を出発点に README / CLAUDE.md 等の整備も担当側で）。
- Kaneko の秘書エージェント（AA リポ）は**開発に関与しない**。連絡・判断はすべて Kaneko 経由。
